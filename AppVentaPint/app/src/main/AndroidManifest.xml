<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jvillegas.appventapint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppVentaPint">
        <activity android:name=".MA_Expresionismo"></activity>
        <activity android:name=".MA_Impresionismo" />
        <activity android:name=".MA_Surrealismo" />
        <activity android:name=".MA_Compra" />
        <activity android:name=".MA_Vista_Compra" />
        <activity android:name=".MA_Hiperrealismo" />
        <activity android:name=".MA_realismo" />
        <activity android:name=".MA_Productos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>